# QA Oturumu Devir Notu — 2026-05-23

Önceki oturumun devamı. Odak: production QA + Sales Manager yetenek
sorgulama + işletme listesi temizliği.

## 🎯 Bu Oturumda Ortaya Çıkan Kararlar

1. **Sales Manager'a şu an soru sorulmayacak** — kullanıcı kararı
   - Doğruluk kanıtı yok (ground truth karşılaştırması yapılmadı)
   - Frontend'den kapatılmayacak; ama doğrulanana kadar güvenilmez

2. **Sales Manager v2 vizyonu (kullanıcıdan)**
   - Aylık hedef: **250.000 TL**
   - Hizmet portföyü + fiyatlar agent'ın "bilmesi" gereken
   - Reactive değil **goal-driven** olmalı
   - Kalıp cevap ≠ yetenek; gerçek aksiyon almalı

3. **İşletme listesi temizliği**
   - Kullanıcı manuel sildi
   - Saklanacaklar: **astro.soma, mindID Reklam Ajansı, MindID**
   - Test hesabı: **MindID**
   - Kontrol aşamasında kaldı (Firestore erişimim yok)

## 🔍 QA Planı (Öncelik Sırasıyla)

### P0 — Production Kırılma Riski
- Cloud Run smoke test (`/health`, orchestrator routing, OpenAI key)
- mind-id Durum Paneli (9/9 yeşil + admin RBAC)
- NocoDB HTTPS + IP whitelist + Caddy

### P1 — Veri Bütünlüğü
- NocoDB ↔ mind-agent senkron
- Auth & yetkilendirme
- Zernio webhook HMAC (borç)

### P2 — Fonksiyonel Regresyon
- Orchestrator agent routing (her sub-agent için 1 test prompt)
- n8n workflow execution log
- mind-id UI E2E

### P3 — Performans & Güvenlik Audit
- Cold start / TTFB / NocoDB query latency
- Secret Manager migration gap
- DR drill (rollback komutu, NocoDB volume backup)

## 🐛 Tespit Edilen Sorunlar

### Sales Manager'da "Gereksiz Yüklenmiş" Tool'lar
9 tool boş/tanımsız:

| Tool | Sorun |
|---|---|
| `get_product_catalog` | brand_identity hiçbir işletmede dolu değil |
| `get_target_audience` | aynı |
| `get_brand_voice` | aynı |
| `get_unique_value_proposition` | aynı |
| `get_sales_playbook` | aynı |
| `ask_reklam_uzmani` | sub-agent token sömürür, değer az |
| `pipeline_forecast` | Fırsatlar tablosu boş |
| `update_sales_memory` | spec yok |
| `weekly_kpi` | **hedef tanımsız** — en kritik eksik |

### İşletme Listesi Şişkinliği (kullanıcı sildi)
Tespit edilen pattern'ler (sebep):
- 4× "Yeni İşletme" → form validation yok
- 4× Slowdays varyantı → duplicate detection yok
- URL kazara name field'a → input sanitization yok
- Test/prod ayrımı yok → `isTest` flag eksik

## 🔒 Bilinen Güvenlik Borçları (önceki oturumdan)

- NocoDB API token chat history'de leak — rotate edilmedi
- Eski OpenAI key leak (yeni key Cloud Run'da)
- mind-agent secrets plain text Cloud Run env (Secret Manager'a taşınmadı)
- Zernio HMAC webhook verify yok (soft mode)
- Firestore rules per-collection sıkılaştırma yok

## ⚠️ Önemli Mimari Tespit

**Frontend → Orchestrator → Sales Manager** akışı keyword-routing
kullanıyor. "Test" diye yazsan da agent gerçek iş sanıyor.
Doğrulama altyapısı (ground truth comparison) **henüz yok**.

## 📋 Sıradaki Sprintler İçin Öneri

### Sales Manager v2 Refactor (3-4 saat)
1. **NocoDB'ye `sales_config` tablosu** (monthly_target_tl=250000, services[], pricing)
2. **9 boş tool sil**, yerine 3 ekle:
   - `get_monthly_target_progress()`
   - `get_pricing()`
   - `suggest_next_actions()`
3. **System prompt'a hedef + fiyat enjekte**
4. **Ground truth test scripti** (her deploy sonrası 10 altın soru)

### mind-id Frontend Koruma (1 saat)
1. Form validation: `name` zorunlu, min 2 char, max 100
2. Duplicate detection: lowercase trim karşılaştırma
3. Soft delete + `isTest`/`isArchived` flag (`types/firebase.ts`)
4. Selector filter: archived/test gizlensin
5. Admin toggle: "Test verilerini göster"

## 🔗 Bağlantılı Dokümanlar
- `DEVIR-MASTER-2026-05-22.md` — önceki oturum devri
- `ARCHITECTURE.md` — orkestra haritası
- `mind-agent/docs/versions.json` — deploy checklist
- `mind-agent/src/agents/sales/sales_manager_agent.py` — agent tanımı
- `mind-agent/src/tools/sales/` — 28 tool kaynak kodu

## 🎬 Yeni Oturumda İlk Adım

Kullanıcının onayı geldikten sonra:
1. **İşletme listesi son halini doğrula** (3 işletme kaldı mı)
2. **Sales Manager v2 sprintini başlat** (sales_config tablosu + tool refactor)
3. **VEYA** ground truth test scriptini önce yaz (Sales Manager'a güven inşa)

---
*QA oturumu: 2026-05-23, Seyma + Claude (Opus 4.7)*
